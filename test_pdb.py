import pdb 
def linear_search(l,key):

	for value in l:
		if key == value:
			return True
	else:
		return False

pdb.set_trace()
l = [10,20,30,40,50,60]
key = 500
print(linear_search(l,key))


(ll)ist => get next lines of code
n => execute the next line 
c => continue 
r => return 
s => step 
   
